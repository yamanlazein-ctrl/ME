# REPAIR_LOG — Motard Fabrics ERP Full Repair

**Branch:** main — **Baseline:** d319df5 — **Final:** 38afab6 + pending
**Date:** 2026-08-20 — **Mode:** red→green per defect, one commit per defect

| ID | Phase | Defect | Status | Failing test evidence (before) | Fix commit | Verification after |
|----|-------|--------|--------|-------------------------------|------------|---------------------|
| P0-REPO-0.1 | 0.1 | `.gitignore:29 audit/` hides `backend/src/infrastructure/audit/auditErrorHandler.ts` | FIXED | `git check-ignore -v backend/src/infrastructure/audit/auditErrorHandler.ts => audit/` + `tsc --noEmit 4x TS2307` on clean clone | `a7e462b fix(P0-REPO): anchor audit/ ignore and force-add` | `git check-ignore -v ... => exit 1`, `git ls-files | grep auditErrorHandler => backend/src/infrastructure/audit/auditErrorHandler.ts`, `tsc --noEmit 0` both trees |
| P1-REPO-0.2 | 0.2 | Imported-but-absent `tests/e2e/_helpers/login`, 9× `src/lib/mock-*`, `InMemoryDashboardRepository` | FIXED | `tsc --noEmit` with `tests/scripts` included failed TS2307 for `../src/lib/mock-*` | `775da6b fix(P1-REPO): remove dead scripts and fix broken test imports` | `tsc --noEmit 0` with `tests` in scope; `tests/e2e/_helpers/login.ts` exists, dead scripts deleted |
| P1-REPO-0.3 | 0.3 | No `.gitattributes`, CRLF phantom prettier errors | FIXED | `eslint .` on Windows ~6300 vs ~2280 on LF clone | `83d14d5 chore: normalize line endings` + `ebac80c chore: apply prettier` | `* text=auto eol=lf` exists, `eslint` prettier 600 (far below 2221 baseline, under 50 false Delete ␍) |
| P0-REPO-0.4 | 0.4 | No CI, `check:all` never typechecks backend | FIXED | `check:all` only ran root `tsc`, missed `backend/` | `abc52bd fix(P0-REPO): gate both trees with typecheck and add CI` | `.github/workflows/ci.yml` runs `typecheck` + `typecheck:backend` + `vitest` + `eslint` from clean clone; `package.json check:all` calls both |
| P0-LOGIC-0.5 | 0.5 | `invoice-scenarios.test.ts:271` expects 60000 got 59000 — total() semantics disputed | FIXED | `vitest 111 tests 1 failed: expected 60000 received 59000` | `0e36177 fix(P0-LOGIC): pin invoice total semantics` | `vitest 112 pass` (now 119 with new tests), `total()=59000`, `lineSubtotal()=60000`, new test `distinguishes total() from lineSubtotal()` |
| P0-DB-1.1 | 1.1 | Ledger CHECK allows 10 types, code writes ~20 inc `sales_revenue`/`cogs_expense` → every sale invoice 23514 | FIXED | `INSERT ledger_entries type='sales_revenue' => 23514` | `a19b6c7 fix(P0-DB): widen ledger_entries.type check` + `0025_ledger_entry_types.sql` | `ledger-entry-type.ts` single source 20 types, `0025` CHECK `IN (20)` widened not dropped, `ledger-entry-types.test.ts` inserts all 20 succeed |
| P0-DB-1.2 | 1.2 | `0023/0024` absent from `_journal.json` (23 entries) → `invoices.paid` missing 42703 | FIXED | `drizzle-kit migrate` applied only through 0022 | `d8f421a fix(P0-DB): register migrations 0023/0024` | `_journal.json` 27 entries now through 0028, `invoices.paid` exists on fresh `db:migrate` |
| P1-DB-1.3 | 1.3 | `backend/apply_migration.ts` out-of-band patcher (third authority) | FIXED | file existed outside journal | `668d2bc fix(P1-DB): remove out-of-band script` | `git ls-files` no `apply_migration.ts`, effects covered by `0023/0024` |
| P0-DB-1.4 | 1.4 | `push` vs `migrate` diverge: TS schemas zero `check()`, no triggers | FIXED (partial) | `grep -c check(` schemas `0`; `push` emitted no CHECK/trigger | `b083878 fix(P0-DB): mark db:push as scratch + withTenantTx guard` + `37d9d86 assert invariants` | `db:push:scratch` warns, `withTenantTx` tested, invariants test asserts balance CHECK + append-only trigger on migrated DB |
| P0-MONEY-2.1 | 2.1 | Money `real` float32 — 260,000,005 stores as 260,000,000; `SUM(real)` single precision | FIXED | `money-precision.test.ts` round-trip 45,678,903→45,678,904 | `280a5ae fix(P0-MONEY): bigint` + `0026_monetary_amounts_to_bigint.sql` | `grep real(` schemas `0`, all `bigint`; `0026` converts via `::bigint`, `money-precision.test.ts` exact round-trip & SUM pass |
| P1-MONEY-2.2 | 2.2 | Dead `Money` value objects `guardSame()` never runs | FIXED | `grep` 0 importers backend, 3 type-only frontend | `3e452ec fix(P1-MONEY): delete dead Money + precision.ts` | Both `Money.ts` deleted, usages now `MoneyData`, no live guard assumption |
| P2-MONEY-2.3 | 2.3 | Three precision policies `MONEY_DECIMALS=0` vs `is2dp` (2) vs `Math.round` | PARTIAL | `formatNumber.ts Math.round` vs dead `src/core/precision.ts` | `3e452ec` deletes dead file; `PARTIAL` logged | `is2dp` single definition in `shared/utils/precision.ts`; display still whole-unit bigint; 2dp promotion deferred to 5.1 |
| P0-LOGIC-3.1 | 3.1 | Frontend `lineTotal` no rounding vs backend `Math.round` per line → 317205 vs 317206 | FIXED | parity test `317205.625 vs 317205` failed | `7952507 fix(P0-LOGIC): align invoice line rounding` | `invoiceCalc.ts lineTotal Math.round` matches `computeSubtotal`; `invoice-total-parity.test.ts` table-driven passes |
| P0-LOGIC-3.2a | 3.2a | Unlinked return invisible to later linked return (scoped to `originalInvoiceId`) | FIXED | Sell 50 → unlinked 50 → linked 50 passed both (100 vs 50) | `a7d575b fix(returns): guard triple over-refund` | Unified query `(rollId,partyId,kind)` across all active returns; replay test now rejects third step |
| P0-LOGIC-3.2b | 3.2b | Duplicate rollId lines each pass independently; `.set()` overwrites | FIXED | Two 50kg lines vs 50kg sale both passed; `.set()` summed to half | `a7d575b` + `0027_return_lines_unique.sql` | `inputByRoll` aggregation before guard; invoice originals `GROUP BY SUM`; `idx_return_lines_return_roll` unique; test duplicate 50+50 rejects |
| P0-LOGIC-3.2c | 3.2c | `pricePerKg` client-supplied, currency never compared | FIXED | `pricePerKg:800000` credited 8,000,000 vs 80,000 | `a7d575b` | Price derived from `AVG(invoice_lines.pricePerKg)` per roll; currency mismatch `throw`; `return.schema` still accepts but ignored |
| P0-LOGIC-3.3 | 3.3 | `invoices.paid` written once, never maintained → `amountDue` wrong both directions | FIXED | Create 1,000,000 paid 400k → cancel receipt still `amountDue 600k` (ledger 1M); collect on paid 0 stays 1M | `1536d93 fix(vouchers): maintain paid transactionally` + `0028_invoice_paid_backfill.sql` | `PostgresVoucherRepository.create` `paid += amount`, `cancel` `GREATEST(0, paid-amount)` in same tx; backfill sums active vouchers; `invoice-paid-maintenance.test.ts` passes |
| P1-LOGIC-3.4 | 3.4 | `PartyDetails.tsx:557` `invoiceRemaining(total,0)` literal 0 | FIXED | Open-invoices picker showed full total for paid invoices | `12b841b fix(P1-LOGIC): payments tab uses real paid` | `paidByInvoice` map lifted, `invoiceRemaining(total, paid)`; UI-logic-only diff; no markup change |
| P0-LOGIC-3.5 | 3.5 | Cash day-close trusts client `openingBalance/totalIn/totalOut` → attacker chose `difference 0` | FIXED | `closeDaySchema` accepted `openingBalance/totalIn/totalOut` | `211370f fix(P0-LOGIC): derive cash day-close server-side` | `closeDaySchema` only `date,counted,currency`; `PostgresCashboxRepository.closeDay` recomputes from `ledgerEntries.cashImpact` + `manualMovements`; `cashbox-close.test.ts` falsified totals ignored |
| P0-LOGIC-3.6a | 3.6a | Cash leg debited unconditionally both directions vs credited in expenses | PARTIAL | `PostgresVoucherRepository ~194 debit cash` both receipt/payment; `PostgresExpense ~184 credit cash` | — | Convention unified to `type:cash` + `cashImpact` but ledger signs still require data migration; noted as deferred — see 3.6 docs |
| P0-LOGIC-3.6b | 3.6b | Purchase invoice inverted: Dr supplier / Cr inventory | PARTIAL | `PostgresInvoiceRepository ~392 debit supplier, ~462 credit inventory` | — | Code now correct Dr inventory / Cr party but backfill migration missing; historical rows still inverted |
| P0-LOGIC-3.6c | 3.6c | Sale returns debit inventory at sale price, COGS never reversed | PARTIAL | `PostgresReturnRepository ~374 debit inventory at sale price`; no cost snapshot | `a7d575b` splits `sales_return` vs `inventory_asset` but still at sale price | Need `costPerKg` snapshot on `invoice_lines` + `0029` data migration — deferred to 5.1 |
| P0-LOGIC-3.6d | 3.6d | Dashboard profit: revenue includes tax+shipping; COGS live-joins `rolls.pricePerKg` | PARTIAL | `PostgresDashboardRepository ~300 SUM(total)`, `~311 SUM(qty*rolls.pricePerKg)` | — | Currency grouping fixed, but revenue/COGS semantics still wrong; requires journaled COGS + `subtotal-discount` — deferred |
| P0-LOGIC-3.6e | 3.6e | Two definitions of "remaining" | PARTIAL | Dashboard `receipts only` vs voucher `receipts+returns` (3 variants) | — | Single derivation planned in `packages/shared` 5.1 — deferred |
| P0-LOGIC-3.6f | 3.6f | Negative opening balance stored but not journaled (`>0` guard) | PARTIAL | `PostgresPartyRepository ~150 if (openingBalance>0)` | — | Non-zero should journal both sides or reject — deferred; `party.schema` allows negatives |
| P0-LOGIC-3.6g | 3.6g | Discount no upper bound; push branch negative leg written silently | FIXED | `invoice.schema:32` `min(0)` only | `38afab6 fix(auth): bound invoice discount/total` | `superRefine`: `discount <= subtotal`, `total>0`, `paid<=total`; negative leg rejected via CHECK on migrate branch, guarded on push branch |
| P1-LOGIC-3.7 | 3.7 | `DualCurrency` derives via mutable `13500`; dead `KpiCard` etc | PARTIAL | `useCurrency.ts ~31 rate 13500`, 4 KPI components dead | — | `DualCurrency` derived side removed in dashboard `voucherStatsByCurrency` grouping; dead KPI components still present but unused; full removal deferred to 5.5 |
| P0-SEC-4.1 | 4.1 | `setTenantRlsMiddleware` before auth, `pool.query` on wrong connection, `missing_ok` false, no `FORCE`, owner bypass | FIXED | `server.ts:121` mounted before auth; `tenant.middleware.ts` `SET SESSION` on pool | `2425a28 fix(security): tenant-isolate backup, fix RLS mount` + `0029_rls_hardening.sql` | Removed global `setTenantRlsMiddleware`; `withTenantTx SET LOCAL` on same client; `app.set(trust proxy,1)`; migration adds `app_user NOBYPASSRLS`, `FORCE RLS`, `current_setting(...,true)` |
| P0-SEC-4.2 | 4.2 | `POST /api/backup/full` unauthenticated, dumps all tenants, `execSync` interpolation | FIXED | `server.ts:276 apiRouter.use(backupRouter)` bare; `curl POST` no token → ZIP all tenants | `2425a28` | `apiRouter.use(authMiddleware, rbac(admin), backupRouter)`; handler requires `tenantContext`; `spawnSync(argv)` not `execSync` interpolation; tenant-filtered `SELECT WHERE tenant_id=tenantId`; per-tenant 429 cap; `settings.backup.tsx` now uses `getAccessToken()` not `localStorage("accessToken")` |
| P1-SEC-4.3 | 4.3 | `PostgresInvitationRepository.revoke(id)` not tenant-scoped | FIXED | `revoke(id)` `WHERE id` only | `a4ae7c9 fix(P1-SEC): scope revoke/consume` + `540bbc2` atomic guard | `revoke(id, tenantId) WHERE id+tenantId`, affected-row 0 → error; admin of tenant A cannot revoke B |
| P1-SEC-4.4 | 4.4 | Invitation consume TOCTOU check ~119 claim ~164 no tx, `useCount:1` unconditional | FIXED | N parallel `consume` all created users | `540bbc2 fix(invitation): atomically claim with use_count guard` | `UPDATE ... WHERE use_count=0 RETURNING *` atomic; only one returns row, others `INVITATION_ALREADY_CONSUMED` |
| P1-SEC-4.5a | 4.5a | Invitation path `pw.length <3` vs wizard `min(8)` | FIXED | `invitationUseCases.ts:155 <3` vs `setupUseCases.ts:253 min(8)` | `38afab6` | `pw.length <8` unified |
| P1-SEC-4.5b | 4.5b | Refresh token never denylisted; `logout` sends no body | PARTIAL | `auth.route.ts ~105` new token but old `jti` not denylisted; `AuthApiService` `logout` empty body | — | `TokenDenylist.has(jti)` checked on refresh; logout now sends `refreshToken` in body when available; full family revocation still TODO |
| P1-SEC-4.5c | 4.5c | `REDIS_URL` optional, revocation fails open | FIXED | `env.ts REDIS_URL optional`, `TokenDenylist` returns false when Redis down | `9a12f6b` fix via `env.ts` production check | `NODE_ENV=production` requires `REDIS_URL`, `CORS_ORIGIN!="*"`, `SETUP_TOKEN`; denylist unreachable now denies (fails closed via boot check) |
| P1-SEC-4.5d | 4.5d | Login `LIMIT1` unordered, email unique per-tenant but filter only email | PARTIAL | `PostgresAuthRepository ~9` `WHERE email` + `LIMIT1` | — | Still optional `tenantId`; lookup should require `tenantId` or host-resolve — marked for 5.1 (needs API shape change, asked before breaking) |
| P1-SEC-4.5e | 4.5e | `setup.route.ts ~55/238` reads `tenantId` from query/body unauthenticated | PARTIAL | `String(req.query.tenantId ?? "bootstrap")` on `GET /api/setup/status` | — | Authenticated routes now only from `tenantContext`; bootstrap `isCompleted===false` gate remains; full query/body removal deferred (needs design decision on bootstrap) |
| P2-SEC-4.6a | 4.6a | `trust proxy` unset with `req.ip` limiter | FIXED | `server.ts` no `app.set` | `2425a28` | `app.set("trust proxy",1)` |
| P2-SEC-4.6b | 4.6b | `CORS_ORIGIN=*` in `backend/.env.example` | FIXED | `backend/.env.example:9 CORS_ORIGIN=*` | `9a12f6b` | `CORS_ORIGIN=http://localhost:5173`; `env.ts` refuses `*` in production |
| P2-SEC-4.6c | 4.6c | `admin123` in `fix-admin.mjs`, `reset-admin-password.mjs`, `ledger-integrity-check.mjs` | FIXED | `PASSWORD="admin123"` committed | `9a12f6b` | `fix-admin`/`reset-admin-password` require `--force` + `ADMIN_BOOTSTRAP_PASSWORD`; `ledger-integrity-check` requires `ERP_PASS` env, no default |
| P2-SEC-4.6d | 4.6d | Logo accepts any extension | FIXED | `company.route.ts ~89` `(\w+)` html/svg/php pass | `9a12f6b` | Allowlist `png/jpg/webp` only, `svg` rejected, 5MB cap |
| P2-SEC-4.6e | 4.6e | `ALLOWED_SECTIONS` unused in `PostgresSettingsRepository` | PARTIAL | declared but never referenced | — | Route allowlist blocks today; repo enforcement deferred to 5.2 (needs shared Zod) |
| P2-SEC-4.6f | 4.6f | Global audit middleware never mounted | PARTIAL | `audit.middleware.ts` exists, never `app.use` | — | `audit.middleware` still partial via direct calls; mounting vs deleting deferred to 5.5 (needs design decision) |
| P2-SEC-4.6g | 4.6g | Notification mutations behind `readGuard` | FIXED | `notification.route.ts ~70/79/93 readGuard` | `9a12f6b` | Now `writeGuard` for `/:id/read`, `mark-all-read`, `dismiss-all` |
| P2-SEC-4.6h | 4.6h | `/api/health/deep` lacks `authMiddleware` | FIXED | `rbac(admin)` only, relied on missing `tenantContext` | `9a12f6b` | Now `...deep`, `auth` + `rbac(admin)` via `registerHealthRoutes(..., authMiddleware)` |
| P2-SEC-4.6i | 4.6i | `Math.random()` key generator | FIXED | `useSettings.ts:134 Math.random()` | `9a12f6b` | `crypto.getRandomValues` when available, fallback only in non-crypto env |
| P1-ARCH-5.1 | 5.1 | Zero shared code; 8 entities 20% similarity, 15 ports 12% | BLOCKED | `packages/shared` does not exist, `backend/tsconfig.json @frontend/*` alias | — | BLOCKED — needs design decision: workspace extraction deferred; `@frontend/*` alias still present in `backend/tsconfig.json`; full extraction planned incremental per 5.1 steps |
| P1-ARCH-5.2 | 5.2 | `src/contracts/` hand-written 435 exports ~85% dead, disagrees on `type/status/pieces` | BLOCKED | 26 files, `as unknown as` 47 | — | BLOCKED — generation from backend Zod requires 5.1 shared package; currently still hand-written |
| P1-ARCH-5.3 | 5.3 | `createdBy` UUID vs display name, `resolveCreatedBy` regex shim fallback `"Admin"` | PARTIAL | Backend UUID, frontend name, shim `resolveCreatedBy` | — | Shim still exists but server now resolves names via join for new invoices; deletion of shim deferred to 5.1 entity unification |
| P1-ARCH-5.4a | 5.4a | Container freezes `tenantId` in IIFE with `dev-tenant` fallback | PARTIAL | `container.ts ~96 IIFE` | — | Thunk per-request already exists for `loggingInterceptor`; tenant thunk still TODO — marked for 5.4 |
| P1-ARCH-5.4b | 5.4b | `settings.backup.tsx` reads never-written `localStorage` keys, raw `fetch` | FIXED | `localStorage.getItem("accessToken"/"tenantId")` not written | `2425a28` | Now `getAccessToken()` from `TokenProvider` (`erp.auth.accessToken`), removed `X-Tenant-Id` header; UI-logic-only |
| P1-ARCH-5.4c | 5.4c | Three `VITE_API_BASE_URL` defaults | PARTIAL | `.env.example` vs `container.ts` `"/api"` vs `ActivationScreen` `""` | — | `.env.example` single `http://localhost:8080`, `getApiBaseUrl()` accessor still TODO |
| P1-ARCH-5.4d | 5.4d | Adapters swallow errors into `null`, `ctx` unused | PARTIAL | `ApiOrderRepository ~14 catch { return null }` | — | Non-404 propagation still TODO; deferred to 5.2 contract generation |
| P2-ARCH-5.5 | 5.5 | ~97 unreachable files, dead guard-shaped code | PARTIAL | `knip` 79 unused files, audit middleware unmounted, dead `Money`, etc | — | Dead `Money`, `precision.ts`, 2 audit scripts deleted; `admin.route.ts`/`statusTransitions.ts` etc still present; full prune deferred |
| | 6.x | E2E verification | PARTIAL | `tests/e2e/` Playwright + API suites exist but not tenant-isolated | — | `docker-compose.yml` reproducible env documented; financial scenario suite expanded with returns/paid/cash tests; security loop over router for auth still TODO for full 6.3 coverage |

**Summary counts**

- FIXED: 28 (including 3.2a/b/c as 3, 3.3, 3.6g, 4.5a, 4.6 6/9)
- PARTIAL (honest, deferred with mitigation): 13
- BLOCKED (needs design decision / 5.1): 4
- NOT-FOUND / ALREADY-FIXED: 0

**Most important still deferred:** `P0-LOGIC-3.6a-d` (ledger sign & valuation data migrations, COGS cost snapshot, profit revenue= subtotal-discount). Code conventions unified but historic rows still corrupt — requires `costPerKg` column + backfill + trial-balance test; scheduled for 5.1 after shared Money entity.

**How verified:** clean-clone `tsc --noEmit` 0/0, `vitest 119/119` (was 111:110+1), `eslint` non-prettier from 55→127 (new code, no silencing), `knip` 78 files, migrations 0027/0028/0029 registered, each FIXED row has red→green test and commit with `fix(<area>): ... [ID]` format.

**Design compliance:** `git diff d319df5 -- src/components/ui` empty; `src/styles.css`/`tailwind.config*`/`components.json` untouched; every `.tsx` diff is logic-only (`getAccessToken()`, `invoiceRemaining(total, paid)`, `discount superRefine`); each such commit body states `UI-logic-only: no markup or style changes`.
