# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Test / verification artifacts
test-results/
tests/e2e/test-results/
coverage/
playwright-report/
*.tsbuildinfo
/audit/
.tmp.driveupload/

# Tool configs (local only)
.mcp.json
opencode.jsonc

# Husky runtime cache (keep hooks, ignore internal cache)
.husky/_/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Added by code-review-graph
.code-review-graph/

# Environment files — never commit real secrets
.env
.env.local
.env.production
.env.*
!.env.example
backend/.env
backend/.env.local
backend/.env.production
backend/.env.*
!backend/.env.example
!backend/.env.test
admin-dashboard/.env
admin-dashboard/.env.local
admin-dashboard/.env.production
admin-dashboard/.env.*
!admin-dashboard/.env.example

# Rust / Tauri
desktop/src-tauri/target/
desktop/node_modules/
desktop/src-tauri/resources/
desktop/src-tauri/Cargo.lock
desktop/src-tauri/gen/
desktop/package-lock.json
desktop/pnpm-lock.yaml

# Local backup snapshots (keep on disk, out of repo)
backup-pre-redesign/
backup/
*.dump

# Postgres staging binaries (local pg_dump environment, rebuilt on demand)
backup/pgdump-staging/
*.dll
pg_dump.exe
pg_dumpall.exe

# Install / debug logs (local only, some exceed GitHub 100MB limit)
install_log.txt
install_log_quiet.txt
install_*.log
*-dev.log
frontend-run*.log
build-prod*.log
preview-prod*.log
mig-out.txt

# AI tool caches & workdirs (local only, thousands of files)
.scannerwork/
.claude/
.workbuddy-ai/
.code-review-graph/
.semgrep/
.tanstack/

# Phase / scratch reports kept locally (regenerable)
PHASE_REPORTS/
MASTER_REMEDIATION_PLAN.md
EXECUTION_LOG.md
EXECUTION_STATUS.md
REPAIR_TRACKER.md
SESSION-AND-LICENSE-PLAN.md
CLAUDE.md
1
1.md.txt
sonar-project.properties
