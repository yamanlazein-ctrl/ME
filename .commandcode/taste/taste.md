- Prefers communicating and receiving reports in Arabic. Confidence: 0.95
- Verifies actual code state before making changes rather than assuming prior progress or trusting a plan's accuracy. Confidence: 0.95
- Wants an explicit per-item audit report (✅/⚠️/❌) delivered before any code is touched. Confidence: 0.9
- When asked to "just check/verify" a plan or feature, treats it as a read-only audit and reports mismatches without modifying code. Confidence: 0.8
- When asked to "just verify/check" (e.g. "فقط تحقق"), does a read-only audit and reports discrepancies without modifying any code. Confidence: 0.8
- Executes provided plans literally without summarizing, skipping, or simplifying; respects exact file names, line numbers, and business logic. Confidence: 0.9
- Stops and asks before proceeding when a plan conflicts with the actual code, instead of assuming and continuing. Confidence: 0.9
- Treats production ledger/inventory changes conservatively: single transaction with row locks (FOR UPDATE), and never breaks existing triggers or protection logic. Confidence: 0.9
- After each completed part, provides a clear summary of changed files + reasons and verifies no build or type errors before considering it done. Confidence: 0.9
- Before starting the project, checks how it is meant to run (README, .env files, docker-compose) and verifies the runtime environment (Docker daemon, Postgres/Redis reachability, migration status, port bindings) rather than assuming it is ready. Confidence: 0.85
- Runs long-lived dev servers in the background and confirms they are actually up via health endpoints, logs, and port checks before reporting success. Confidence: 0.85
- When something won't start, diagnoses from the actual environment/process state (log files, process command lines, env vars, listening ports) rather than guessing or assuming. Confidence: 0.8
- When a feature seems missing/removed, prefers restoring the exact original implementation (same component names, same data bindings) recovered via git history rather than reimplementing it differently. Confidence: 0.85
- Investigates feature regressions with git archaeology (git log / git blame / git show) to pinpoint exactly which commit changed what before concluding. Confidence: 0.8
- When asked to fix a UI/layout/duplication issue and the user explicitly declares it is "layout/duplication cleanup only," keeps the change strictly UI-only — no data/hook/API modifications. Confidence: 0.85
- When removing duplicate UI elements/cards (and their now-dead source components), removes them entirely — deleting unused components and adjusting the grid column layout so the grid reflows cleanly without a gap — rather than merely hiding them (e.g. CSS/display:none). Confidence: 0.8
- Prefers prominent, clearly-labeled text buttons over small icon-only buttons for key actions, placed directly beside the relevant field so users can discover them without searching. Confidence: 0.9
- When asked to verify that a feature "really works", prefers practical runtime verification via a real headless browser (Playwright/Chromium) — actually clicking the control and inspecting the DOM — rather than only reading code, and reports concrete before/after evidence plus an explicit PASS/FAIL result. Confidence: 0.85
- Uses TODO lists (todo_write) to plan and track multi-part verification/implementation work. Confidence: 0.8
- Verifies audit claims against the actual codebase rather than trusting a supplied plan, and treats E2E harness paths outside the repo as unverifiable rather than assuming they exist. Confidence: 0.8
- When asked to create test/seed data in a real database, isolates it with clear TEST-ONLY markers (name and notes), neutral values (creditLimit 0, openingBalance 0), and confirms the record via both API and a direct DB query before reporting success. Confidence: 0.7
- For demo/test seeding, prefers a full clean reset (delete all existing business data, keeping only infrastructure like users/tenants/settings) before generating fresh demo data, rather than mixing new data with old, rather than preserving it. Confidence: 0.8
- When the user says they do not want to start with any data ("💄 مابدي ابدأ بينات"), they want ALL business data wiped to zero (stock/fabrics/colors/rolls, customers/suppliers, invoices, vouchers, orders, returns, ledger, stock movements) while keeping the system fully functional (admin login, tenant, numbering counters, settings intact) — an empty clean slate, with no seed/demo data regenerated, and confirmation that login still works afterward. Confidence: 0.85
- When setting up a project for the user to test, expects the assistant to start it and explicitly report the working login credentials (email + password) so the user can log in themselves. Confidence: 0.8
- Wants dead/unrelated artifacts removed when asked to clean the project — stale backup folders, AI plan files, temp inspect/verify scripts and seed backups — while keeping genuinely useful tooling (seed scripts, backups, test specs) and documenting the choice. Confidence: 0.7
- For features involving data persistence, requires end-to-end verification at the database level (real rows/queries), not just a working UI or a successful API call. Confidence: 0.95
- Prefers highly structured, step-by-step instructions and reports with explicit success criteria and pass/fail gates. Confidence: 0.9
- Expects the assistant to fully read and acknowledge context, and confirm access to project files, before taking any action. Confidence: 0.85
- When a blocking issue prevents reaching/loading the feature under investigation (e.g., an auth/tenantId redirect that hides every page), resolves the blocker first as the highest-priority gating item, and refuses to consider the dependent feature verified until it is actually reachable and exercisable in the running app. Confidence: 0.9
- When tracing the scope of a feature or regression, searches the whole codebase for all occurrences rather than assuming the explicitly-listed locations are the complete set (e.g., "check thoroughly, don't assume it's limited to just these"). Confidence: 0.8
- In UI verification tests, prefers waiting for actual rendered content (e.g. waitForSelector for a real row/number) over a fixed sleep/timeout when asserting async-loaded data (React Query tables), treating an arbitrary 2500ms wait that races the data as a test flakiness bug rather than a product failure. Confidence: 0.7
- On this Windows dev box, prefers pointing the frontend API base URL at `127.0.0.1` (IPv4 loopback) instead of `localhost`, because `localhost` resolves to IPv6 `::1` and causes intermittent connection failures — when a service responds on 127.0.0.1 but not localhost, configures it for IPv4 loopback to get stable connectivity. Confidence: 0.7
- For multi-issue feature/UI requests, wants the assistant to diagnose to root cause and lay out a written plan of the planned changes first, then implement only after presenting it ("حط خطة لهل مشاكل ثم اشتغل"). Confidence: 0.75
