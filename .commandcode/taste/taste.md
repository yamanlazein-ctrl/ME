- Prefers communicating and receiving reports in Arabic. Confidence: 0.95
- Verifies actual code state before making changes rather than assuming prior progress or trusting a plan's accuracy. Confidence: 0.95
- Wants an explicit per-item audit report (✅/⚠️/❌) delivered before any code is touched. Confidence: 0.9
- When asked to "just check/verify" a plan or feature, treats it as a read-only audit and reports mismatches without modifying code. Confidence: 0.8
- When asked to "just verify/check" (e.g. "فقط تحقق"), does a read-only audit and reports discrepancies without modifying any code. Confidence: 0.8
- Executes provided plans literally without summarizing, skipping, or simplifying; respects exact file names, line numbers, and business logic. Confidence: 0.9
- Stops and asks before proceeding when a plan conflicts with the actual code, instead of assuming and continuing. Confidence: 0.9
- Treats production ledger/inventory changes conservatively: single transaction with row locks (FOR UPDATE), and never breaks existing triggers or protection logic. Confidence: 0.9
- After each completed part, provides a clear summary of changed files + reasons and verifies no build or type errors before considering it done. Confidence: 0.9
- Before starting the project, checks how it is meant to run (README, .env files, docker-compose) and verifies the runtime environment (Docker daemon, Postgres/Redis reachability, migration status, port bindings) rather than assuming it is ready. Confidence: 0.85
- Runs long-lived dev servers in the background and confirms they are actually up via health endpoints, logs, and port checks before reporting success. Confidence: 0.85
- When something won't start, diagnoses from the actual environment/process state (log files, process command lines, env vars, listening ports) rather than guessing or assuming. Confidence: 0.8
- When a feature seems missing/removed, prefers restoring the exact original implementation (same component names, same data bindings) recovered via git history rather than reimplementing it differently. Confidence: 0.85
- Investigates feature regressions with git archaeology (git log / git blame / git show) to pinpoint exactly which commit changed what before concluding. Confidence: 0.8
- When asked to fix a UI/layout/duplication issue and the user explicitly declares it is "layout/duplication cleanup only," keeps the change strictly UI-only — no data/hook/API modifications. Confidence: 0.85
- When removing duplicate UI elements/cards (and their now-dead source components), removes them entirely — deleting unused components and adjusting the grid column layout so the grid reflows cleanly without a gap — rather than merely hiding them (e.g. CSS/display:none); before deleting a duplicate, first confirms and reports what data each of the duplicates was bound to so the kept element preserves the necessary data. Confidence: 0.85
- Prefers prominent, clearly-labeled text buttons over small icon-only buttons for key actions, placed directly beside the relevant field so users can discover them without searching. Confidence: 0.9
- When asked to verify that a feature "really works", prefers practical runtime verification via a real headless browser (Playwright/Chromium) — actually clicking the control and inspecting the DOM — rather than only reading code, and reports concrete before/after evidence plus an explicit PASS/FAIL result. In QA passes where the user explicitly reserves judgment ("I will judge correctness myself"), drop the PASS/FAIL verdict and report raw evidence only. Confidence: 0.85
- Uses TODO lists (todo_write) to plan and track multi-part verification/implementation work. Confidence: 0.8
- Verifies audit claims against the actual codebase rather than trusting a supplied plan, and treats E2E harness paths outside the repo as unverifiable rather than assuming they exist. Confidence: 0.8
- When asked to create test/seed data in a real database, isolates it with clear TEST-ONLY markers (name and notes), neutral values (creditLimit 0, openingBalance 0), and confirms the record via both API and a direct DB query before reporting success. Confidence: 0.7
- For demo/test seeding, prefers a full clean reset (delete all existing business data, keeping only infrastructure like users/tenants/settings) before generating fresh demo data, rather than mixing new data with old, rather than preserving it. Confidence: 0.8
- When the user says they do not want to start with any data ("💄 مابدي ابدأ بينات"), they want ALL business data wiped to zero (stock/fabrics/colors/rolls, customers/suppliers, invoices, vouchers, orders, returns, ledger, stock movements) while keeping the system fully functional (admin login, tenant, numbering counters, settings intact) — an empty clean slate, with no seed/demo data regenerated, and confirmation that login still works afterward. Confidence: 0.85
- When setting up a project for the user to test, expects the assistant to start it and explicitly report the working login credentials (email + password) so the user can log in themselves. Confidence: 0.8
- Wants dead/unrelated artifacts removed when asked to clean the project — stale backup folders, AI plan files, temp inspect/verify scripts and seed backups — while keeping genuinely useful tooling (seed scripts, backups, test specs) and documenting the choice. Confidence: 0.7
- For features involving data persistence, requires end-to-end verification at the database level (real rows/queries), not just a working UI or a successful API call. Confidence: 0.95
- Prefers highly structured, step-by-step instructions and reports with explicit success criteria and pass/fail gates. Confidence: 0.9
- Expects the assistant to fully read and acknowledge context, and confirm access to project files, before taking any action. Confidence: 0.85
- When a blocking issue prevents reaching/loading the feature under investigation (e.g., an auth/tenantId redirect that hides every page), resolves the blocker first as the highest-priority gating item, and refuses to consider the dependent feature verified until it is actually reachable and exercisable in the running app. Confidence: 0.9
- When tracing the scope of a feature or regression, searches the whole codebase for all occurrences rather than assuming the explicitly-listed locations are the complete set (e.g., "check thoroughly, don't assume it's limited to just these"). Confidence: 0.8
- In UI verification tests, prefers waiting for actual rendered content (e.g. waitForSelector for a real row/number) over a fixed sleep/timeout when asserting async-loaded data (React Query tables), treating an arbitrary 2500ms wait that races the data as a test flakiness bug rather than a product failure. Confidence: 0.7
- On this Windows dev box, prefers pointing the frontend API base URL at `127.0.0.1` (IPv4 loopback) instead of `localhost`, because `localhost` resolves to IPv6 `::1` and causes intermittent connection failures — when a service responds on 127.0.0.1 but not localhost, configures it for IPv4 loopback to get stable connectivity. This also applies to dev servers (e.g. explicitly binding Vite to `--host 127.0.0.1`), since binding to `::1` breaks browser/API connectivity. Confidence: 0.8
- When running a full-lifecycle / destructive E2E scenario, isolates it in a separate clean database (and separate ports) rather than polluting the main dev database, and keeps the real dev DB's existing data intact. Confidence: 0.7
- After a large verification session, cleans up: deletes temporary diagnostic scripts it created, restores any temporarily-modified DB state (passwords, device registrations, seed rows) back to its original values, and leaves the working tree matching the pre-session state except for the intended fix. Confidence: 0.7
- For multi-issue feature/UI requests, wants the assistant to diagnose to root cause and lay out a written plan of the planned changes first, then implement only after presenting it ("حط خطة لهل مشاكل ثم اشتغل"). Confidence: 0.75
- After UI/visual changes, prefers capturing a real screenshot of the running app and using vision/image inspection to confirm what is actually rendered (labels, numbers, dates), rather than only trusting DOM text extraction or Playwright `innerText`. Confidence: 0.8
- When the user explicitly enters "diagnose-only" mode ("لا تصلح أي شيء الآن — تشخيص فقط"), the assistant must not modify any behavior-facing code: only read code, run existing diagnostic scripts as-is, and may add temporary `console.error` for swallowed errors — but only without altering the response returned to the user, and it must explicitly mark the additions as DIAG and note that they should be removed once the root cause is found. Confidence: 0.85
- When a backend layer silently swallows the real exception and returns a generic user-facing error message (e.g. a use-case `try/catch` returning `{ok:false, error:"فشل إنشاء اللون"}`), the assistant must locate the exact file:line that swallows the error and surface the real exception (e.g. `console.error` of the caught value) before drawing any conclusion about the root cause — because the visible error message is unreliable. Confidence: 0.85
- When resuming work that was interrupted by a tool/usage outage, the assistant must not blindly trust handoff notes about "what was discovered" — must re-verify each claim against the current code/git state before acting, and must report literal raw output of any command run rather than summarized descriptions. Confidence: 0.8
- When producing a final report, prefers a structured numbered format (Root cause / per-page status / E2E result, or Diagnosis / Classification / Suggested direction) rather than a free-form narrative. Confidence: 0.8
- When a user asks a focused fix/cleanup question, prefers to answer it directly and concisely without re-running prior multi-test verification suites or opening unrelated investigations. Confidence: 0.75
- When the user gives very specific UI instructions with explicit rules (e.g. "delete the smaller card, adjust the grid so it reflows without a gap", "do not relocate X, only relocate Y"), treats them as literal contract — no scope expansion, no "while we're at it" changes. Confidence: 0.85
- When proposing a non-trivial code change, requires an explicit upfront audit of scope: exact list of files to be touched, per-file reason, and total line count added/removed — before any approval is given, and pushes back if the proposed scope is larger than the stated goal warrants ("ليش 12 ملف و911 سطر لمجرد..."). Confidence: 0.85
- When a multi-step plan is explicitly numbered/listed and concluded with "لا تنتقل لأي خطوة تالية غير المذكورة" (do not proceed to any step not listed), treats that as a hard stop after the listed steps complete — must stop, report, and wait for explicit approval before doing the next logical thing, even if it seems obvious. Confidence: 0.85
- After temporary diagnostic instrumentation (e.g. a `console.error` added during root-cause investigation) has served its purpose, the user expects it removed immediately and the original file state verified clean (e.g. `git diff` empty for those files) — no leftover DIAG/comment traces — before any real fix is proposed. Confidence: 0.8
- The standard "diagnose-only → propose real fix" flow: execute the diagnostic step, surface the root cause with file:line, clean up the temporary instrumentation, then present the proposed real fix as a plan only (not execute it) and wait for explicit approval. Confidence: 0.8
- When documenting a newly-discovered bug/problem (especially to present before fixing, or as a "found but not yet fixed" item in a final report), wants it in the exact structured format Expected / Actual / Evidence / File:Line / Root Cause — not a free-form narrative. Confidence: 0.85
- Refuses to declare something "ready / works 100%" unless every required section actually passes with live evidence; treats assumptions, guesses, and "it should work" claims as disqualifying for a final success verdict, and will instead call out the specific unresolved items. Confidence: 0.85
- After a cross-cutting change (e.g. licensing/auth), expects the full existing automated test suite — all permanent guards — to be run together in a single pass to prove no regression to unrelated functionality (invoices, stock, ledger), rather than only spot-checking the touched area. Confidence: 0.8
- For a persistence/startup fix, proves durability by actually restarting the service (repeatedly, e.g. twice in a row) and re-verifying the surviving artifact each time, rather than relying on code inspection alone. Confidence: 0.7
- Prefers secrets and keys (JWT signing secret, encryption key, license signing key) to be fixed values persisted in `.env` and mandatory at startup — never randomly generated per boot — so signed tokens and encrypted data survive restarts. Confidence: 0.9
- When asked to fix something but investigation shows the code is already correct (no bug actually exists), refrains from changing it just to appear responsive and instead documents the verified-correct state with live evidence rather than introducing an unnecessary change. Confidence: 0.75
- When reporting bugs, distinguishes observed behavior from proven root cause: cites exact file:line when the cause is actually pinned down, but explicitly marks unresolved causes as "root cause not established" rather than speculating on a cause. Confidence: 0.75
- When a negative test scenario targets an invalid state (e.g., fabric/color mismatch on an invoice line), verifies both the UI-level guard and the backend protection; if the UI itself prevents constructing the scenario, documents it as "not buildable via the UI" (with the backend protection line as evidence) rather than forcing it via a workaround or marking it a failed test. Confidence: 0.7
- When producing a comprehensive final bug/issue list (e.g., at the end of a diagnostic session), wants a single numbered table — one issue per row — with columns: رقم | المشكلة | أين لوحظت (Block/Step) | خطورة فعلية | File:Line (if proven) | Root Cause (only if proven, else "not established"); never merges similar problems into one row, never omits minor/cosmetic issues already reported in partial reports, and never adds unproven new issues. Confidence: 0.8
- Classifies issue severity using the taxonomy "تفسد بيانات / وظيفة معطلة / عرض مربك / تجميلي فقط" (data-corrupting / broken function / confusing display / cosmetic only). Confidence: 0.75

